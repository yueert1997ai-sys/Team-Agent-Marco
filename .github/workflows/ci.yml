name: CI
on:
  pull_request:
  push:
    branches: [main]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install
        run: npm install
      - name: Compile
        run: npm run build:ts
      - name: Copy UI
        run: npm run copy:ui
      - name: Council tests
        run: node --test tests/council.test.mjs tests/final-budget.test.mjs
      - name: Provider tests
        run: node --test tests/providers.test.mjs
      - name: Settings tests
        run: node --test tests/settings.test.mjs
      - name: UI tests
        run: node --test tests/ui-smoke.test.mjs
      - name: Mock flow test
        run: node --test tests/demo-cli.test.mjs
